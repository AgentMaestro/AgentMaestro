*** Begin Patch
*** Update File: agents/consumers.py
@@
-        tool_name = (call.get( name) or ).strip()
+ tool_name = (call.get(name) or ).strip()
+        logger.info(
+            AgentChatConsumer._handle_tool_call start run=%s tool=%s,
+            self.run_id,
+            tool_name,
+        )
@@
-        future = asyncio.get_running_loop().create_future()
-        self._tool_call_waiters[str(tool_call.id)] = future
+        future = asyncio.get_running_loop().create_future()
+        self._tool_call_waiters[str(tool_call.id)] = future
+        logger.info(
+            AgentChatConsumer._handle_tool_call sending tool_request run=%s tool_call_id=%s requires_approval=%s,
+            self.run_id,
+            tool_call.id,
+            requires_approval,
+        )
-        await self.send_json(
+        await self.send_json(
+        logger.info(
+            AgentChatConsumer._handle_tool_call awaiting tool_call_id=%s run=%s,
+            tool_call.id,
+            self.run_id,
+        )
@@
-        result = await future
+        result = await future
+        logger.info(
+            AgentChatConsumer._handle_tool_call future resolved run=%s tool_call_id=%s result_keys=%s,
+            self.run_id,
+            result.get(tool_call_id),
+            sorted(result.keys()),
+        )
*** End Patch
